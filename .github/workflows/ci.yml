name: ci
on:
  workflow_dispatch:
  push:
    branches:
      - develop
      - main
  pull_request:
jobs:
  call-workflow:
    uses: craftcms/.github/.github/workflows/ci.yml@v1
    secrets:
      slack_webhook_url: ${{ secrets.SLACK_PLUGIN_WEBHOOK_URL }}
    with:
      run_ecs: true
      run_phpstan: true
